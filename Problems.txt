El build que construi en Localhost no logra comunicarse con el servidor. Cors?

No puedo mandar emails por Gmail

Cookie:
Si hay mas cookies me falla la función para atrapar cookies [getCookie]
Cookies y Credenciales de Token Caidos completamente

Juego:

Un viejo jugador solo aparece en la pantalla del nuevo jugador cuando se mueve.

Dos instancias tienen monstruos distintos. Problema con el server en como maneja el movmeinto de los monstruos.

Monstruos no tiene limites y salen del juego pero quedan activos.

Base de dato:
problema con email como unique en chat Game manager => newMessage


Ideas:

explorar rooms in sockets para Chat

Guardar el objeto jugador en la base de datos


Para Deploy in Heroku:

cambiar .env en Client (UrlServer & BYPASS_AUTH)
cambiar .env en Server (BYPASS_AUTH)
Run bundle.sh en Client
